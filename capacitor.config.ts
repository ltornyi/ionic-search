import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ltornyi.ionic-search',
  appName: 'ionic-search',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
